# Internal Renderer Modules

Renderer internals for V1 live here and remain private to `@dithered-particle-canvas/react`.

T2+ will replace these placeholder directories with renderer contracts, Canvas2D oracle utilities, WebGL2 backends, interaction state, and shared helpers.
